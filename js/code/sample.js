function example() {
    if (isSpecialDeal()) {
        total = price * 0.95;
        send();
    }
    else {
        total = price * 0.98;
        send();
    }
}



([a-zA-Z_$][a-zA-Z_$0-9]*)\s*=\s*\1\s*\?\s*\1\s*:\s*(.*);
\s*([a-zA-Z_$][a-zA-Z_$0-9]*)\s*\?\s*\1\s*:\s*(.*)

asd = asd ? asd : [];
a = a || [];

